// Orientation toggle component - keeping the interface simple and functional
export function OrientationToggle() {
  // This component is no longer needed as we'll use native device orientation
  return null;
}